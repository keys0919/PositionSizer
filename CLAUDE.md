# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

개인 투자 포지션 비중 계산기 PWA. 총자본 하나만 입력하고 바텀시트에서 상황을 고르면 금액이 나온다.

**개별주 단일 전략 (증분 모델, 전 자본 기준)**: 현행 지침(v10) staged 사이징. 2슬롯 × 슬롯상한 50% = 최대 100%. 상황별 "이번 스텝에 추가할 금액(증분, 총자본 대비)".

각 `inc`는 **총자본 대비 직접 비율**이다. 지침 확정값(초입1차 10 / 눌림1차 35 / 초입1차→2차 40 / 눌림1차→2차 15%, 총자본 기준)은 임의 변경 금지. 예: 200만 → 슬롯 100만.

> **섹터 ETF 오버레이는 2026-08-03 기각**(국면 미스매치·역비대칭, 실전 미투입) → 2026-08-04 앱에서 제거. 근거: INV KB `섹터 오버레이 — 리더 위성 집중 증폭·同방향 상관` / `2026-08-03-브레드스지속성비대칭-오버레이단독집중미스매치검증`. 되살리지 말 것.

## 개발 / 배포

별도 빌드 도구 없음. 파일 수정 후 브라우저에서 직접 열면 된다.

```bash
# 로컬 테스트 (브라우저에서 열기)
open index.html

# 배포
git push  # → GitHub Pages 자동 반영
```

배포 URL: `https://yeon.github.io/position-sizer/`

## 파일 구조

```
position-sizer/
├── index.html        # 전체 앱 (HTML + CSS + JS 단일 파일)
├── manifest.json     # PWA 메타
└── service-worker.js # 오프라인 캐시
```

외부 의존성 없음. 바닐라 JS만 사용한다.

## 핵심 계산 로직

### 계산 공식 (공통)

`금액 = 총자본 × inc`, `총자본 대비 % = inc`.
현재 보유금 입력 없음(직전 차수는 staged 플랜값 가정).

### 개별주 (증분 모델)

**전 자본 기준.** 슬롯당 상한 = 총자본 50%(2슬롯 × 50% = 최대 100%). staged 슬롯 채움 20/70/100%.
**"이번 스텝에 추가할 증분"**을 계산 — 직전 차수는 staged 플랜값 가정.

| 상황(진입 경로) | 비중 이동(총자본) | inc | 총자본 대비 |
|------|------|------|------|
| 초입 1차 | 0 → 10% | 0.10 | +10% |
| 눌림 1차 | 0 → 35% | 0.35 | +35% |
| 초입 1차 → 2차 | 10 → 50% | 0.40 | +40% |
| 눌림 1차 → 2차 | 35 → 50% | 0.15 | +15% |

- **2차 증분은 직전 1차 유형에 의존**(초입출발 +40 vs 눌림출발 +15, 총자본 기준). "2차" 단독 항목 없이 경로로 나열.
- `inc`는 **총자본 대비**(지침 사례73 확정값 유지 — 임의 변경 금지).

### 공통 원칙

- **동적 리밸런싱 없음** → 상황마다 고정%. 슬롯 분기·합산·현금 뷰 불필요.
- 청산(100% EXIT)은 전량 매도라 계산 대상 아님.
- 상수: `SIT` 배열(index.html) 단일 소스. 개별주 사이징 값은 지침 확정분 — 임의 변경 금지.

## 화면 구성

- **입력 존**: 총 자본금 / 상황(바텀시트 원탭 선택 — 4개)
- **결과 존**: 이번 추가액 강조 / 상황 · 비중 이동 · 총자본 대비
- 입력↔결과를 divider·존 헤더로 시각 분리(조건/결과 혼동 방지).

## 개발 원칙

- 과잉 개발 금지 — 증분 계산 외 확장 규칙(슬롯 합산·리밸런싱·현금 뷰 등)은 넣지 않는다
- 단일 파일(index.html) 우선 — 기능 추가 시에도 분리 최소화
- 모바일 전용 UI
